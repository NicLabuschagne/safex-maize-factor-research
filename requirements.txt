# Third-party dependencies for this research project.
# Pin exact versions once the project settles, for reproducibility.
pandas>=2.0
numpy>=1.24
pyarrow>=15.0  # parquet engine for pandas read_parquet/to_parquet
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3
statsmodels>=0.14  # ADF/KPSS, OLS with HAC (Newey-West) standard errors
xgboost>=2.0  # gradient boosting for the factor regression
jupyter>=1.0
akshare>=1.18  # Chinese market data (DCE/CZCE/SHFE futures)

# The shared library is installed separately, in editable mode, so that
# edits to it are picked up here immediately:
#     pip install -e ..\quanttools
